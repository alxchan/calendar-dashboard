@using Google.Apis.Calendar.v3.Data
@{
    ViewData["Title"] = "Calendar";
}

<component type="typeof(CalendarDashboard.Views.Components.MainCalendar)" render-mode="ServerPrerendered" param-name="@( "hi" )" param-time="@(new EventDateTime 
{ 
    DateTimeDateTimeOffset = System.DateTime.Now, 
    TimeZone = TimeZoneInfo.Local.Id 
})" />
